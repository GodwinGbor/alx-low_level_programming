#include "main.h"

/**
 * _islower - checks for the lowercase char
 * @c: the char to be checked
 * Return: 1 if c is lowercase, then 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
