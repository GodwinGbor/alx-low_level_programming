#include "main.h"

/**
 * _abs - to computes with absolute value of the integer
 * @n: int to check
 * Return: absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
